# MSFD — pinned to the `cdvd` conda environment on the server (Python 3.9.25).
#
# PyTorch is built against the machine's CUDA runtime; install the matching
# wheels first (see https://pytorch.org/get-started/previous-versions/), e.g.
#   pip install torch==2.8.0 torchvision==0.23.0
# then `pip install -r requirements.txt` for the rest.

# --- core deep-learning stack ---
torch==2.8.0
torchvision==0.23.0
torchmetrics==1.8.2
pytorchvideo==0.1.5         # torch.hub I3D-R50 / SlowFast backbones
fvcore==0.1.5.post20221221 # inference-FLOPs counting
iopath==0.1.10

# --- numerics / data ---
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
scikit-learn==1.6.1

# --- vision / IO / augmentation ---
opencv-python==4.12.0.88
Pillow==11.3.0
albumentations==2.0.8
einops==0.8.1
matplotlib==3.9.4

# --- misc utils ---
PyYAML==6.0.3              # FTCN config (models/FTCN/*.yaml)
tqdm==4.67.1
simplejson==4.1.1
tabulate==0.9.0
termcolor==3.1.0
